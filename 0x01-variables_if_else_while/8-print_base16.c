#include <stdio.h>
/**
 * main - Point Entry
 * Return: Always 0 Readable
 */

int main(void)
{
        char c;

	int b;

        c = 'a';
	b = 0;
        while
                (b < 0) {
                        putchar(b + '0');
                        b++;
                }
	while
		(c <= 'f') {
			putchar(c);
			c++;
		}
        putchar('\n');
        return (0);
}
