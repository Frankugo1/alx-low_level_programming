#include <stdio.h>
/**
 * main - Point Entry
 * Return: Always 0 Readable
 */

int main(void)
{
        char k;

	int b;

        k = 'a';
	b = 0;
        while
                (b < 0) {
                        putchar(b + '0');
                        b++;
                }
	while
		(k <= 'p') {
			putchar(k);
			k++;
		}
        putchar('\n');
        return (0);
}
