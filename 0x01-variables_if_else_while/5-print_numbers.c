#include <stdio.h>
/**
 * main - Point Entry for Variable
 * Return: Always 0 (Readable)
 */

int main(void)
{

	int a; 

	for (a = 0; a < 10; a++)
	        printf("%d", a);
	putchar('\n');
	return (0);
}
