#include <stdio.h>
/**
 * main - Point of entry for variable
 * Return: Always 0 (readable)
 */

int main(void)
{

	int a; 

	for (a = 0; a < 10; a++)
	printf("%d", a);
	putchar('\n');
	return (0);
}
