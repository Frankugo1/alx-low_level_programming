#include <stdio.h>

/**
 * main - point of entry
 * Return: Always 0 (readable)
 */

int main(void)
{
        char c;
	char f;
	
        c = 'a';
	f = 'A';
        while

         (c <= 'z') {
                  putchar(c);
                  c++;
                }
	while
		(f <= 'Z') {
			putchar(f);
			f++;
		}
        putchar('\n');
        return (0);
}
