#include <stdio.h>
   
/**
 * main - point of entry
 * Return: Always 0 (readable)
 */

int main(void)
{
        char c;

	c = 'a';
	while
         (c <= 'z') 
        {
            putchar(c);
            c++;
	}
	putchar('\n');
	return (0);
}
