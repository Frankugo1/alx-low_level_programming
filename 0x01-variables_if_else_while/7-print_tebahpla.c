#include <stdio.h>
/**
 * main - Point Entry
 * Return: Always 0 Readable
 */

int main(void)
{
        char k;

        k = 'z';
        while
                (k >= 'a') {
                        putchar(k);
                        k--;
                }
        putchar('\n');
        return (0);
}
